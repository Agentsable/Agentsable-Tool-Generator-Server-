# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.claude-trace

# Secrets — never commit. Mirrored in .claudeignore/.cursorignore/.llmignore
# (which hide them from AI assistants) and .claude/settings.json permissions.deny
# (which is what Claude Code actually enforces).
.env
.env.*
*.env
